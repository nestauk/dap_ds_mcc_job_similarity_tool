# Find top 10 matches for every job
# Find which ones aren't symetric

# Can you build a graph, e.g if the similarity is X create a graph
# Look at comparing the ranking between the 4 different factors, which agree and which disagree
# Look at other similairity scores, e.g job title
# Do obviously just look at top 5 for all jobs, 